import React from 'react';
import ReactDOM from 'react-dom';
import './index.html';

const root = document.createElement('div');
document.body.appendChild(root);

ReactDOM.render(<h1>Bilt + React</h1>, root);
